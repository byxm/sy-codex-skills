<!-- Generated by init-harness -->
# 缺陷回流模板

本模板用于记录交付后暴露出的典型问题。它不替代 [人工反馈模板](./verification-feedback.template.md) 或 [分层评估模板](./evaluation.template.md)，而是用来沉淀“为什么没提前挡住、后续要补到哪里”，并为长期记忆提供正式来源。

## 使用说明

- 一次性小修不必强制创建回流单。
- 典型问题、重复问题、影响多人协作的问题，建议单独创建回流单。
- 回流单的目标不是复述修 bug 过程，而是提炼根因、漏挡点和补强动作。
- 只有真正可跨任务复用的结论，才建议回写到长期记忆。
- 建议把回流单放在当前任务目录下的 `defects/` 中，方便和对应任务的简报、评估与反馈放在一起。

## 模板

### 缺陷标题

填写说明：用一句话概括本次交付后问题，例如“某字段回填文案与实际绑定字段不一致”。

### 关联任务简报

填写说明：填写对应任务简报的文件名或链接。

### 关联评估单

填写说明：填写对应评估单的文件名或链接；如果当时没有单独评估单，可说明原因。

### 关联反馈单

填写说明：填写关联的反馈单文件名或链接；如有多份，可列多个。

### 问题现象

填写说明：概述交付后暴露出的现象和触发条件，重点写清对用户或协作方的可感知结果。

### 影响范围

填写说明：说明问题影响单个场景、同类功能、还是跨模块/多人协作链路。

### 根因归类

填写说明：从以下推荐值中选择一个最接近的归类：`需求理解偏差`、`方案判断错误`、`改动范围扩张失控`、`链路漏改`、`浏览器事实未核实`、`人工验收遗漏`、`review 未覆盖`、`其他`。

### 当时为什么没提前挡住

填写说明：写清当时是哪个环节没挡住，例如任务简报边界没更新、评估只看了页面没看 Network、review 没覆盖新增路径等。

### 本次修正策略

填写说明：写清本次准备如何修正问题，重点描述修正方向，不需要展开所有实现细节。

### 后续补强动作

填写说明：至少从以下落点中选择一个：`更新 memory`、`更新 task brief 使用规则`、`更新 evaluation 检查项`、`更新 review 关注点`、`更新 feedback 提示语`。如有多个，可同时填写。

### 是否回写长期记忆

填写说明：填写“是”或“否”。

### 回写内容摘要

填写说明：如果决定回写长期记忆，写清准备回写的稳定结论；如果不回写，可写“不适合跨任务复用”。
