<!-- Generated by init-harness -->
# 外部资料模板

本模板用于记录任务中依赖的外部在线文档、内部组件文档、接口文档、设计规范等资料。在线资料会变化，不应直接把旧摘要当作长期事实；当前任务依赖它时，应重新确认资料状态。

## 使用说明

- 每个重要外部资料建议单独建立一份引用记录。
- 本模板记录“本次任务实际依据了什么资料”，不替代长期记忆。
- 长期记忆只记录资料入口、适用场景和稳定使用规则。
- 如果资料无法访问，应让用户提供截图、摘录或可访问链接，并在本记录中说明。

## 模板

### 资料名称

填写说明：写清资料名称，例如“某组件库 Select 组件内部文档”。

### 资料类型

填写说明：从以下推荐值中选择：`在线文档`、`内部 Wiki`、`组件文档`、`接口文档`、`设计规范`、`其他`。

### 资料地址

填写说明：填写 URL、内网地址、文档路径或用户提供资料的位置。

### 访问权限

填写说明：说明资料是公开、内网、登录后可见、需要 VPN，还是需要用户提供截图/摘录。

### 适用范围

填写说明：说明这个资料适用于哪些组件、模块、页面或任务。

### 最后确认时间

填写说明：记录本次实际确认资料的日期和时间；在线资料每次任务依赖时都应重新确认。

### 引用时的版本或页面状态

填写说明：记录资料版本、页面标题、组件版本、接口版本，或“未标注版本”等状态。

### 关键结论摘要

填写说明：只摘录本次任务真正依赖的结论，例如关键属性、参数、默认值、限制条件、迁移说明。

### 易变内容

填写说明：记录最可能变化的部分，例如 API 参数、组件属性、默认值、废弃说明、权限规则。

### 使用规则

填写说明：写清后续任务使用该资料时的规则，例如“每次开发前必须重新打开确认”“不能只依赖旧摘要”。

### 关联任务

填写说明：填写引用该资料的任务简报、评估单或反馈单。

### 是否需要回写长期记忆

填写说明：填写“是”或“否”。只有资料入口、适用场景或长期稳定经验适合回写长期记忆。

### 回写内容摘要

填写说明：如果需要回写长期记忆，写清准备回写的稳定结论；如果不回写，可写“不适合跨任务复用”。
