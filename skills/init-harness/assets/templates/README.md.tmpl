<!-- Generated by init-harness -->
# {project_name} 轻量 Harness

这套轻量 Harness 用来解决两个实际问题：一是在大工程里保持上下文稳定，二是在人工验证后让 Agent 稳定吸收错误反馈并继续修正。它不追求完整自治，不包含脚本、Skill、自动截图或完整端到端验证。

所有文档都使用中文。任务细节不要直接堆到长期记忆里，截图和现象描述是人工反馈的主输入。

## 文档说明

- [长期记忆](./memory/{memory_file_name})：记录跨任务稳定事实、热点入口、修改规律和已知坑。
- [任务简报模板](./templates/task-brief.template.md)：每个需求开始前复制一份，用来冻结当前任务目标、范围、入口、假设和验证方式。
- [人工反馈模板](./templates/verification-feedback.template.md)：验证发现问题后按固定格式补充事实，降低多轮反馈时的漂移。
- [Code Review 模板](./templates/code-review.template.md)：功能开发完成后可单独复制一份，用来记录独立评审发现、风险判断和修正建议。
- [分层评估模板](./templates/evaluation.template.md)：每个任务维护一份，用来按轮次记录静态检查、结构检查、浏览器事实核验和人工验收结果。
- [缺陷回流模板](./templates/defect-loop.template.md)：交付后遇到典型问题时单独成文，用来沉淀根因、漏挡点和后续补强动作。
- [外部资料模板](./templates/external-reference.template.md)：记录在线文档、内部组件文档、接口文档等可变资料的引用方式和本次确认结论。

## 目录结构

- `memory/`：长期稳定记忆，按工程或领域沉淀高复用事实。
- `templates/`：任务简报模板、反馈模板、Code Review 模板、分层评估模板等通用文档模板。
- `tasks/`：所有任务实例的统一入口。每个任务都应在自己的子目录下存放简报、评估、评审、反馈和缺陷回流资料。
- `references/`：外部在线文档、内部组件文档、接口文档等资料引用记录。
- `README.md`：当前 Harness 的总入口和使用规则。

推荐的任务目录结构如下：

```text
tasks/
  <task-id>/
    task-brief.md
    evaluation.md
    code-review.md
    feedback/
      verification-feedback-xxx.md
    defects/
      defect-loop-xxx.md
```

这里的核心原则是：任务实例按任务归档，不按文档类型散落。这样后续回到某个旧任务继续开发、修 bug 或做回归时，只需要进入该任务目录即可恢复完整上下文。

## 推荐使用顺序

1. 开始需求前先阅读 [长期记忆](./memory/{memory_file_name})。
2. 基于 [任务简报模板](./templates/task-brief.template.md) 新建一份当前需求的任务简报。
3. 如果任务依赖外部在线文档或内部组件文档，先基于 [外部资料模板](./templates/external-reference.template.md) 建立或更新引用记录。
4. 在 `tasks/<task-id>/` 下建立当前任务目录，并把任务简报、评估单、评审记录、反馈单和缺陷回流都归到这个目录里。
5. 为当前任务创建一份 [分层评估单](./templates/evaluation.template.md)，并在任务简报中引用它。
6. 设定任务初始状态为 `需求澄清`，随后按阶段推进任务简报。
7. 编码、分析和讨论过程中只围绕任务简报推进，避免在对话里重复堆背景。
8. 功能开发完成后，先做一次独立 code review，再进入验证和最终交付。
9. 验证有问题时，按 [人工反馈模板](./templates/verification-feedback.template.md) 补充单轮事实，并把整轮结果汇总到评估单。
10. 交付后若暴露出典型问题，按 [缺陷回流模板](./templates/defect-loop.template.md) 单独成文，再决定是否回写长期记忆和规则。
11. 需求结束后，只把可复用结论回写到 [长期记忆](./memory/{memory_file_name})，不要回写单任务细节。

## 实际使用步骤

### 1. 新开对话时先加载 Harness 上下文

在新对话开始时，先让 Agent 阅读：

- [README.md](./README.md)
- [长期记忆](./memory/{memory_file_name})

这一步的目标不是重新理解整个工程，而是先掌握 Harness 的使用方式，并优先利用长期记忆里的热点路径和稳定结论快速定位代码。

### 2. 收到需求后先创建任务简报

当需求描述、图片、原型、截图或其他参考资料提供完成后，不要直接开始写代码。先基于 [任务简报模板](./templates/task-brief.template.md) 创建一份当前任务的具体开发文档，明确：

- 本次要实现什么
- 成功标准是什么
- 本次范围和明确不做的部分是什么
- 已确认入口和关键代码路径是什么
- 当前实现假设和验证方式是什么
- 当前任务处于哪个阶段、当前阶段目标是什么、什么情况下需要回退
- 初始改动边界是什么、如果后续改动面扩张应如何留痕
- 是否依赖外部在线资料、内部组件文档或接口文档

创建任务简报后，建议同时基于 [分层评估模板](./templates/evaluation.template.md) 建一份当前任务的评估单，并把文件名或链接写入任务简报。这样后续 review、浏览器核验和人工验收都能有固定落点。

建议从一开始就先确定当前任务目录，例如 `tasks/task-brief-call-agent-action-2026-05-11/` 或 `tasks/feature-xxx-2026-05-11/`，然后把：

- `task-brief.md`
- `evaluation.md`
- `code-review.md`
- `feedback/`
- `defects/`

都放在这个任务目录下。这样后续任务接力、路径迁移、回归修复和交付后问题回流，都不会散落到多个平级目录里。

如果任务依赖可变在线资料，应基于 [外部资料模板](./templates/external-reference.template.md) 建立或更新引用记录。不要把在线文档的旧摘要直接当作长期事实；当前任务依赖它时，应重新确认文档入口、访问时间、关键结论和易变内容。

确认这份任务简报没有明显偏差后，再进入代码开发。后续分析、编码、讨论和修正，都优先围绕这份任务简报推进，而不是把关键信息散落在聊天上下文里。

如果一个任务跨越多轮、多天，或者当前对话发生了上下文压缩，那么继续任务前应先重新读取对应的任务简报，再继续分析、编码或调试。这样做的目的不是依赖模型长期记忆，而是通过重新加载本地任务文档恢复稳定上下文。

### 3. 开发过程中优先直接做本地检查

在 Agent 写代码过程中，能直接做的阅读、定位、类型检查、已有测试或其他本地检查，优先直接做，不需要为每次小验证都创建额外文档。

[人工反馈模板](./templates/verification-feedback.template.md) 不是用来记录所有验证动作的，而是用来记录已经明确的问题事实，尤其适用于：

- 人工验收后发现的界面或交互问题
- 需要跨多轮调试保留的问题现象
- 会影响后续判断和修正方向的问题反馈

任务推进过程中，应持续维护任务简报中的“当前任务状态”和“阶段切换记录”。如果当前阶段目标已经完成，就显式切换到下一个阶段；如果发现目标理解错误、成功标准变化或方案明显不成立，就不要继续在当前阶段局部打补丁，而要先把状态回退到更合适的阶段。

同时应维护任务简报中的边界字段。这里的边界控制不是要求一开始就知道所有要改的文件，而是要求：

- 初始边界来自已确认入口和关键代码路径
- 如果实现过程中必须改到新的目录、模块或链路，应先在任务简报的“边界扩张记录”里补充原因，再继续推进
- 真正要控制的是“没有证据地顺手改一片”，不是“进入陌生模块”这件事本身

### 4. 人工验证发现问题后再记录反馈

当人工验证发现问题时，再基于 [人工反馈模板](./templates/verification-feedback.template.md) 创建或补充一份结构化反馈，记录：

- 已确认事实
- 操作路径
- 期望结果
- 实际结果
- 证据
- 影响范围

Agent 收到反馈后，先重述问题，再开始修改代码。如果这轮反馈只是补充事实，就继续沿当前任务简报推进；如果这轮反馈推翻了原来的实现假设，就先更新任务简报中的假设或风险，再继续分析和修改。

对于重要的人工反馈，不要只在任务简报里写一句摘要，而应先形成一份独立的反馈单。任务简报中的“反馈记录”只保留轮次摘要、当前状态以及对应反馈单的文件名或链接，用来追踪过程，不替代完整的结构化反馈内容。

如果人工反馈中包含“我怀疑”“可能是”“你看看是不是”这类猜测，应把它单独放在反馈单的“我的猜测（可错）”中，不要和已确认事实写在一起。Agent 必须先回到代码、状态流、日志、接口数据或浏览器现场中验证这个猜测，只有验证成立后才能按这个方向修改代码；如果验证不成立，应明确说明“猜测未被证实”，并回到事实继续排查。

### 4.1 任务状态机与回退规则

这套 Harness 里的任务状态机是轻量流程控制，不是自动代码回滚系统。回退默认指“流程回退”和“任务认知回退”，即先更新任务简报中的状态、目标、范围和假设，再决定现有代码哪些保留、哪些重做。

- `需求澄清`：目标是把需求、成功标准和范围说清楚。新任务默认从这里开始；如果中途发现原始需求描述错误、成功标准改变或任务边界变了，也应先回退到这里。
- `方案确认`：目标是确认实现路径、入口、关键链路和主要风险。任务简报稳定后进入这里；如果验证阶段发现整体实现方向不成立，应回退到这里重新定方案。
- `开发中`：目标是按当前方案完成代码实现和基础自检。方案确认后进入这里；如果 review 发现只是实现细节问题、目标不变，则回退到这里修正。
- `待评审`：目标是用独立视角做 code review。功能完成且基础自检完成后进入这里；如果 review 发现问题，回退到 `开发中`。
- `待验证`：目标是完成分层评估和人工验收。review 修正完成后进入这里；如果验证只暴露局部问题，可回退到 `开发中` 局部修正；如果验证发现需求理解错误或方案严重偏离，应回退到 `方案确认` 或 `需求澄清`。
- `待交付`：目标是确认本轮可以交付。分层评估通过后进入这里；如果交付前又发现明显风险，可回退到 `待评审` 或 `待验证`。
- `已完成`：目标是收尾、回写长期记忆并归档。交付完成后进入这里。

简单判断规则：

- 如果“任务目标”不变，只是实现细节、联动逻辑、默认值或显示结果有问题，优先局部修正并回退到 `开发中`。
- 如果“成功标准 / 本次范围 / 当前实现假设”已经明显失效，就不要继续局部修补，应回退到 `方案确认` 或 `需求澄清`。
- 不要把“状态回退”理解成必须立刻撤销所有代码；先修正任务认知，再决定代码如何处理。

### 4.2 分层评估如何使用

[分层评估模板](./templates/evaluation.template.md) 用来记录“这一轮完整检查得出了什么结论”，它和任务简报、反馈单分工不同：

- 任务简报：记录任务目标、当前状态和阶段推进。
- 人工反馈模板：记录某一轮问题的事实、证据和猜测。
- 分层评估单：记录一整轮静态检查、结构检查、浏览器事实核验和人工验收的结果与结论。

分层评估固定包含 4 层：

- `静态检查`：例如 lint、typecheck、build、已有测试，目标是挡住低级错误和明显编译问题。
- `结构检查`：例如关键链路是否改全、入口是否一致、配置/状态/校验是否漏改，目标是减少“表面能跑但链路漏改”的问题。
- `浏览器事实核验`：例如 Console、Network、DOM、页面快照，目标是核实运行时事实，减少错误猜测。
- `人工验收`：例如 UI 效果、联动体验、业务结果，目标是由人工最终判断是否符合预期。
- `边界一致性检查`：目标是确认当前代码改动面是否仍与任务简报一致；如果已经扩张，是否补充了边界扩张记录。

使用原则固定如下：

- 每个任务维护一份评估单，按轮次追加，不为每次小验证都新建文件。
- 每轮评估写清“检查了什么、通过了什么、哪一层未通过、下一步准备怎么做”。
- 重要运行问题仍先进入 [人工反馈模板](./templates/verification-feedback.template.md)，评估单只汇总这一轮的结果和结论。
- 任务简报中的“验证方式”保留为策略说明；真正的评估结果沉淀在评估单。
- 如果当前实现已超出登记边界，但任务简报中没有对应的扩张记录，则本轮评估不应直接判定为可交付。
- 如果任务依赖外部资料，本轮评估应确认资料是否已重新访问、引用记录是否更新、实现是否仍符合当前文档。

### 5. 功能完成后先做独立 code review

当功能已经实现、基础自测也已通过时，不建议直接交付，而应补一次独立 code review。这里的重点不是复述实现思路，而是用独立视角检查：

- 有没有潜在 bug 或边界条件遗漏
- 有没有跨模块回归风险
- 有没有对不熟悉模块造成潜在影响
- 有没有明显的过度修改、无关修改或可以收敛的改动
- 有没有缺少必要校验、测试或异常处理

对 review 的建议做法是：

- 尽量在独立 Agent 会话中进行，减少“作者视角”对判断的干扰
- review 前先读取当前任务简报、相关反馈记录和本次改动
- 需要单独沉淀评审结果时，复制 [Code Review 模板](./templates/code-review.template.md) 生成独立评审记录
- 明确要求 Agent 按 code review 模式输出问题，而不是重讲实现过程
- review 发现的问题先记录到任务简报的“评审记录”，再决定是否回到当前任务继续修正
- review 时应顺带检查边界是否失控：有没有改到未登记的新路径、有没有无证据扩张、有没有可以收敛的无关改动

### 6. 任务结束后再回写长期记忆

不要在任务进行中频繁补写 [长期记忆](./memory/{memory_file_name})。只有在任务完成后，且确认某条结论满足以下条件时，才回写：

- 这是跨任务可复用的稳定事实
- 后续同类需求大概率还会用到

像单次调试过程、临时问题、一次性判断，不应直接写入长期记忆，应保留在任务简报或反馈记录中。

如果问题已经发生在交付后，且属于典型问题、重复问题或会影响多人协作的问题，应优先基于 [缺陷回流模板](./templates/defect-loop.template.md) 建一份回流单，再决定是否回写长期记忆。长期记忆更适合记录“已经被回流提炼过的稳定结论”，而不是直接记录某次故障本身。

## 任务简报复用规则

### 什么时候继续使用同一个任务简报

如果后续补充、修正或调试仍然服务于同一个任务目标，就继续使用同一个任务简报。

判断方式可以简单一些：如果“任务目标”这一句话不用改，通常就继续沿用同一个任务简报。

### 什么时候应该新建任务简报

如果后续补充已经改变了任务目标、成功标准或本次范围，就应新建一个任务简报。

判断方式同样尽量简单：如果“任务目标 / 成功标准 / 本次范围”已经明显变化，就新建任务简报，不要继续在旧任务里叠加。

### 结构重构后如何做任务接力

如果一个功能已经做了大部分，但中途需要为了结构整洁度做明显重构，例如单文件过大、职责混杂、路径需要重新拆分，此时不建议继续复用原任务简报做结构改造，而应拆成“任务接力”：

1. 原任务保留业务目标、已完成进度和剩余功能范围。
2. 新建一个重构任务，专门记录结构拆分、职责迁移和路径变化。
3. 重构完成后，如仍需继续剩余功能，再新建一个续做任务，基于重构后的结构继续推进。

这样分工的原因是：

- 原任务更适合保留“要做什么”的历史上下文
- 重构任务更适合保留“代码结构现在长什么样”的权威信息
- 续做任务更适合保留“基于新结构还剩什么没完成”

继续开发时的阅读顺序建议固定为：

1. 当前工程的 README 和长期记忆
2. 最新的结构权威任务
3. 原始业务任务
4. 相关 feedback / evaluation / defect-loop

不要要求 AI 把旧任务完整改写成最新结构。旧任务只需要补充“已被后续任务接管”的说明、后续任务链接，以及“原路径信息已过时，以新任务为准”的提示。

### 任务实例为什么要按任务目录归档

随着功能越来越多，如果把任务简报、反馈单、评估单和缺陷回流都按类型扔在顶层目录里，后面回到某个旧任务继续开发或修 bug 时，通常会遇到两个问题：

- 很难快速判断哪些反馈、评估和缺陷属于同一个任务
- 结构重构、任务接力或跨轮回归后，很难恢复当时的完整上下文

因此脚手架默认采用的规则应是：

- `memory / references / templates` 保持顶层共享
- 所有任务实例统一进入 `tasks/`
- 每个任务在自己的子目录中维护 `task-brief / evaluation / code-review / feedback / defects`

这样后续只要进入某个任务目录，就能看到这个任务从立项、开发、评审、验证到缺陷回流的完整材料。

### 功能补充与验证反馈如何区分

功能性补充、需求变更或范围调整，应直接更新当前任务简报中的以下部分：

- `任务目标`
- `成功标准`
- `本次范围`
- `当前实现假设`

验证中发现的问题、调试反馈或人工验收结果，应优先按 [人工反馈模板](./templates/verification-feedback.template.md) 记录，再把摘要回填到当前任务简报的“反馈记录”中。

如果反馈足够重要，或者后续还会多轮追踪，应优先保留独立反馈单。任务简报里只写摘要和引用，不要让“反馈记录”变成丢失操作路径、期望结果和证据的简化流水账。

### Code Review 与验证反馈如何区分

这套 Harness 里，code review 和验证反馈不是同一件事：

- code review 关注的是代码层面的潜在问题，例如回归风险、边界条件、隐性副作用、过度修改和缺失校验
- 验证反馈关注的是运行结果是否符合预期，例如界面显示不对、联动错误、保存结果错误、接口数据异常

推荐顺序是：

1. 功能开发完成后先做独立 code review
2. 根据 review 结果修正代码
3. 再进入人工验证和多轮反馈

如果交付后又暴露出问题，也可以继续追加 review，尤其是当修改开始扩散到不熟悉模块时。不要把 review 记录直接混写进“反馈记录”；评审结论应写到任务简报的“评审记录”，验证问题继续写“反馈记录”。

### 分层评估与验证反馈如何区分

- `verification-feedback` 记录的是单轮问题事实，适合描述“哪里不对、操作路径是什么、证据是什么”。
- `evaluation` 记录的是整轮评估结论，适合说明“这一轮静态检查、结构检查、浏览器核验和人工验收分别怎么样”。

如果你已经明确发现了一个具体现象，先写反馈单；如果你要总结“这一轮验到哪里了、卡在哪一层”，写评估单。

### 缺陷回流与长期记忆如何区分

- `defect-loop` 记录的是交付后典型问题的回流过程，关注“为什么没提前挡住、后续要补到哪里”。
- `memory` 记录的是跨任务复用的稳定结论，关注“以后再遇到类似问题，应直接记住什么”。

推荐链路是：

1. 交付后发现问题
2. 如果只是一次性小修，可直接修正，不必强制建回流单
3. 如果属于典型问题、重复问题、多人协作风险问题，先建 `defect-loop`
4. 在回流单里判断根因、漏挡点和补强动作
5. 只有真正可复用的结论才回写到 `memory`

这样 `memory` 的新增内容会更稳，不容易被单次噪音污染。

### 外部资料与长期记忆如何区分

外部在线文档、内部组件文档、接口文档和设计规范都可能随时变化，不适合直接整段沉淀进长期记忆。推荐链路是：

1. `memory` 只记录资料入口、适用场景和使用规则
2. `references` 记录某次任务实际访问的地址、时间、关键结论和易变内容
3. `task brief` 引用本次使用的外部资料记录
4. `evaluation` 检查当前实现是否仍符合本次确认的资料状态
5. 只有长期稳定的使用经验，才从 references 或任务结论回写到 `memory`

使用规则：

- 当前任务依赖在线资料时，Agent 不应默认旧摘要仍然正确。
- 修改代码前应重新打开或重新读取资料；如果无法访问，应让用户提供截图、摘录或可访问链接。
- 回答、方案或代码修改中依赖外部资料时，应说明本次依据的链接、访问时间和关键结论。
- 如果资料更新导致实现变化，应在外部资料记录、任务简报或评估单中写清楚。

### 渐进式边界控制如何理解

这套 Harness 里的边界控制不是白名单制度，而是“边改边留痕”：

- 不要求开发者在陌生功能里一开始就知道所有要改的目录
- 允许随着理解加深逐步扩张边界
- 但只要扩张，就要在任务简报中写明扩到了哪里、为什么必须扩、如果不扩会卡住什么

可以把它理解成：

- `task brief` 负责记录边界从哪里开始、怎么扩张
- `review` 负责检查扩张是否合理
- `evaluation` 负责确认当前实现和登记边界是否一致
- `defect-loop` 负责总结“为什么这次边界控制没挡住问题”

### 旧任务与新任务如何同步

当结构重构导致路径发生变化时，不要把旧任务当作当前代码结构真相。推荐做法是：

- 旧任务：
  只补充当前状态、后续任务链接、剩余功能摘要，以及“原路径信息已过时”的说明。
- 新任务：
  负责记录当前结构权威来源、路径迁移记录、拆分后的职责边界和新的关键路径。

也就是说，旧任务保留历史，新任务承接现实。后续 Agent 继续开发时，应优先读取新任务中的结构信息，而不是继续沿着旧路径推断。

### 如何发起一次有效的 code review

当你让 Agent 做 review 时，建议显式提供以下上下文：

- 当前任务简报
- 相关反馈记录
- 本次改动的文件范围或 diff
- 需要重点关注的风险点，例如某个不熟悉模块、跨工程依赖、回填链路、状态同步链路

### 人工猜测与代码事实如何区分

人工反馈里常常会带有排查思路，但这些思路不一定正确。为了避免 Agent 顺着错误猜测过度修改代码，应固定以下规则：

- 用户提供的截图、操作路径、期望结果、实际结果、日志和接口现象属于事实输入。
- 用户提供的“怀疑原因”“可能是某个字段”“你看看是不是某个开关”属于待验证假设，不属于实现指令。
- Agent 收到这种反馈后，先拆分“事实”和“猜测”，再决定排查顺序。
- 任何基于猜测的大范围改动，都必须先由代码证据、日志证据或浏览器证据支持。
- 如果猜测未被证实，Agent 应明确说明猜测不成立，并继续基于事实排查，而不是继续顺着猜测修改。

### 浏览器侧验证原则

当问题边界已经缩小到“前端运行时到底发生了什么”时，让 Agent 自己看浏览器 Console、Network 和 DOM 是合适的。浏览器侧验证的目标是核实事实，不是替代人工验收。

- 人工负责提供复杂交互路径、截图和验收标准
- Agent 负责在必要时用浏览器工具核实运行时事实
- 浏览器工具用于减少错误猜测，不用于替代人工做完整回归

{optional_browser_section}

## 使用边界

- 这套 Harness 适合大工程中的局部功能新增和改造。
- 界面正确性以人工截图、录屏和现象描述验收为主。
- 可自动执行的检查只作为辅助，不替代人工反馈。
