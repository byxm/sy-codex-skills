<!-- Generated by init-harness -->
# 分层评估模板

开始评估前先看当前任务简报和相关反馈记录。本模板用于按任务维度维护一份持续更新的评估单，用来记录每一轮静态检查、结构检查、浏览器事实核验和人工验收的结果。

## 使用说明

- 每个任务维护一份评估单，按轮次追加，不为每次小验证都新建一份文件。
- 评估单记录的是“这一轮完整检查的结论”，不是替代 [人工反馈模板](./verification-feedback.template.md) 的问题单。
- 重要运行问题仍应先进入反馈单，再在评估单中汇总本轮结果。
- 任务简报中应引用这份评估单，方便跨轮次继续任务时快速恢复状态。
- 建议把评估单放在当前任务目录中，例如 `tasks/<task-id>/evaluation.md`。

## 模板

### 评估名称

填写说明：用一句话说明当前评估对象，例如“某功能分层评估”。

### 关联任务简报

填写说明：填写对应任务简报的文件名或链接。

### 当前评估轮次

填写说明：按轮次递增，例如“第 1 轮”“第 2 轮”。

### 评估目标

填写说明：写清这一轮主要想确认什么，例如“确认 review 修正后可进入交付”“确认接口联调后页面回填正确”。

### 静态检查结果

填写说明：记录 lint、typecheck、build、已有测试等结果。写清执行了什么、通过了什么、哪些没有执行。

### 结构检查结果

填写说明：记录关键链路是否改全，例如入口、状态、配置、校验、表单联动、保存链路是否一致。

### 浏览器事实核验结果

填写说明：记录 Console、Network、DOM、页面快照等浏览器侧事实。写清验证了什么、看到什么证据、排除了什么猜测。

### 边界一致性检查结果

填写说明：记录当前代码改动面是否仍与任务简报一致；如果已经扩张，是否补充了边界扩张记录。

### 外部资料一致性检查结果

填写说明：如果任务依赖外部在线文档、内部组件文档或接口文档，记录本轮是否已重新确认资料、访问时间、关键结论，以及当前实现是否仍符合资料说明。

### 人工验收结果

填写说明：记录人工对页面效果、交互联动、体验和业务结果的验收结论。

### 未通过项

填写说明：汇总本轮仍未通过的层级和问题，优先写对后续决策有影响的项。

### 结论与下一步

填写说明：明确本轮之后应进入哪个任务状态，例如“继续待验证”“回退到开发中”“可进入待交付”。
