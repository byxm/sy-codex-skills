<!-- Generated by init-harness -->
# 人工反馈模板

本模板用于在人工验证后把问题事实结构化反馈给 Agent。反馈应挂到当前任务简报的“反馈记录”中，开始前可先查看 [任务简报模板](./task-brief.template.md)。长期稳定结论不要直接写在这里，应在任务结束后筛选后回写到 [长期记忆](../memory/{memory_file_name})。建议把反馈单放在当前任务目录下的 `feedback/` 中，而不是散落在顶层目录。

## 反馈模板

### 反馈标题

填写说明：用一句话概括本轮问题，例如“行为切换后联动表单未刷新”。

### 问题类型

填写说明：从以下推荐值中选择一个最接近的类型：界面显示、表单联动、默认值错误、保存结果错误、范围判断错误、其他。

### 已确认事实

填写说明：这里只写已经确认的现象，例如截图看到的结果、接口未返回、控制台报错、某字段未回显。不要把猜测原因写在这里。

### 操作路径

填写说明：按用户操作顺序描述路径，最好能让未参与本轮的人复现。

### 期望结果

填写说明：写清理想状态下应看到什么结果。

### 实际结果

填写说明：写清现在实际发生了什么，避免使用“有点不对”这类模糊描述。

### 证据

填写说明：可填写截图路径、录屏说明、控制台报错、接口返回、日志片段等。

### 我的猜测（可错）

填写说明：这里可以写“我怀疑是某个字段名不一致”“可能是某个开关没生效”之类的辅助思路。这里的内容不是实现要求，Agent 必须先验证是否被代码、日志、接口或浏览器现场证实，再决定是否采用。

### 影响范围

填写说明：说明问题只影响当前场景，还是会影响同类控件、同类事件或整个模块。

### 是否阻塞继续

填写说明：填写“阻塞”或“不阻塞”。

### 是否需要浏览器侧验证

填写说明：填写“需要”或“不需要”。当问题涉及接口是否发出、返回值是否正确、DOM 状态是否符合预期、事件是否触发等运行时事实时，优先填写“需要”。

### 补充说明

填写说明：补充上下文、临时判断或额外限制，没有可留空。

## Agent 处理要求

- 收到反馈后先重述问题，不要直接改。
- 重述时必须拆成“事实 / 猜测 / 期望 / 范围 / 证据”五部分。
- 对“我的猜测（可错）”默认按待验证假设处理，不能直接据此改代码。
- 当反馈要求或暗示了某个修改方向时，先验证该方向是否被代码、日志、接口数据或浏览器信息支持。
- 如果需要浏览器侧验证，优先查看 Console、Network 和 DOM/页面快照，再给出修改结论。
- 如果反馈已经能直接定位，按当前任务简报继续推进。
- 如果反馈推翻了原假设，先更新任务简报，再继续分析和修改。
