<!-- Generated by init-harness -->
# 任务简报模板

开始前先看 [长期记忆](../memory/{memory_file_name})。本模板用于冻结单个需求的上下文，避免在对话中重复堆积背景。验证阶段如发现问题，请把事实挂到当前任务简报的“反馈记录”，并参考 [人工反馈模板](./verification-feedback.template.md)。

## 使用说明

- 每个新需求复制本模板生成一份独立任务简报。
- 任务推进过程中优先更新这份文档，而不是在对话中重复重讲背景。
- 任务结束后，从“任务结论”提炼 1 到 3 条可复用结论，再决定是否回写到 [长期记忆](../memory/{memory_file_name})。

## 模板

### 任务名称

填写说明：用一句话说明需求名称，尽量包含模块名和改动目标。

### 当前任务目录

填写说明：填写当前任务实例目录，例如 `tasks/feature-xxx-2026-05-11/`。建议当前任务的简报、评估、评审、反馈和缺陷回流都放在这个目录下。

### 上游任务

填写说明：如果当前任务由前一个任务接力而来，填写上游任务的文件名或链接；如果是独立新任务，可写“无”。

### 任务目标

填写说明：写清本次要解决什么问题，最好控制在 2 到 3 句话内。

### 成功标准

填写说明：写清界面表现、交互结果、保存结果或限制条件，作为完成判断依据。

### 当前任务状态

填写说明：从以下 7 个固定阶段中选择一个：`需求澄清`、`方案确认`、`开发中`、`待评审`、`待验证`、`待交付`、`已完成`。新任务默认从 `需求澄清` 开始。

### 当前阶段目标

填写说明：写清当前阶段最重要的产出，例如“确认成功标准和范围”“完成代码实现并通过基础自检”“完成本轮分层评估”。

### 进入当前阶段的条件

填写说明：写清为什么当前任务已经进入这个阶段，避免阶段切换没有依据。

### 回退触发条件

填写说明：写清出现哪些情况应回退到更早阶段，例如“成功标准变化”“方案明显不成立”“验证发现理解偏差很大”。

### 当前结构权威来源

填写说明：如果当前任务涉及结构重构或接力开发，填写当前最可信的结构来源任务或文档，例如“以 `task-brief-refactor-xxx.md` 为准”。如果当前任务本身就是结构权威来源，可写“当前任务自身”。

### 本次范围

填写说明：列出本次允许修改的模块、页面、交互链路或文档范围。

### 明确不做

填写说明：写清这次不处理的部分，避免需求在中途发散。

### 已确认入口

填写说明：这里只填已证实的入口文件、组件或模块，不写猜测路径。

### 关键代码路径

填写说明：这里只填已证实的关键文件或目录，优先记录本次一定会看的路径。

### 初始改动边界

填写说明：记录任务开始时已确认的改动边界，通常来自“已确认入口”和“关键代码路径”。这里不要求一开始覆盖所有后续改动，只记录当前已经有证据支撑的范围。

### 当前实现假设

填写说明：最多写 5 条，每条都应是当前准备按其推进的实现假设。

### 边界扩张记录

填写说明：按轮次追加记录边界何时扩张、扩到了哪里、为什么必须扩张，以及如果不扩张会卡住什么。若改动面没有扩张，可写“当前无新增边界扩张”。

### 当前边界风险

填写说明：记录当前是否已经进入不熟悉模块、跨目录改动或高回归风险区域，用来提醒 review 和 evaluation 重点关注。

### 路径迁移记录

填写说明：如果当前任务涉及重构、拆分或接力开发，记录旧路径如何迁移到新路径，例如“`old-file.tsx` 拆到 `a.tsx / b.tsx / c.tsx`”。如果没有路径变化，可写“当前无路径迁移”。

### 待验证风险

填写说明：记录当前最可能出错或仍未确认的点，便于验证时重点关注。

### 关联评估单

填写说明：填写当前任务对应的分层评估单文件名或链接。建议每个任务维护一份评估单，并放在当前任务目录中，例如 `tasks/<task-id>/evaluation.md`。

### 关联外部资料

填写说明：填写本任务依赖的外部资料记录文件名或链接，例如 `references/external-reference-xxx.md`。如果依赖在线文档，应记录本次访问时间和关键结论，不能只依赖旧摘要。

### 验证方式

填写说明：说明本次如何验收，区分自动检查和人工验证的分工。

### 评审方式

填写说明：说明本次是否需要单独 code review、由谁发起、重点看哪些风险点。涉及不熟悉模块、跨目录影响或交互链路较长时，应明确把独立 review 写进这里。

### 评审记录

填写说明：按轮次或按日期记录 review 结论。这里只记录评审摘要、风险等级、是否需要修正，以及对应的 review 文档、对话或 diff；不要和“反馈记录”混写。需要独立沉淀时，可基于 [Code Review 模板](./code-review.template.md) 单独成文，建议放在当前任务目录中，例如 `tasks/<task-id>/code-review.md`。

### 反馈记录

填写说明：按轮次追加，例如“第 1 轮 / 第 2 轮”。这里只记录问题摘要、当前状态，以及对应反馈单的文件名或链接；重要人工反馈应先按 [人工反馈模板](./verification-feedback.template.md) 单独成文，再把摘要回填到这里。建议反馈单放在当前任务目录下的 `feedback/` 中。

### 阶段切换记录

填写说明：按时间或轮次记录状态切换，例如“从 `方案确认` 进入 `开发中`”“因验证偏差较大从 `待验证` 回退到 `方案确认`”。这里记录状态变化和原因，不展开写实现细节。

### 后续接力任务

填写说明：如果当前任务后续被重构任务或续做任务接管，填写对应任务的文件名或链接；如果尚未发生接力，可写“当前无”。

### 任务结论

填写说明：任务完成后提炼 1 到 3 条可复用结论，便于后续回写长期记忆。
