### 浏览器侧验证可选章节

如果当前工程后续需要接入浏览器侧验证工具，请根据本机环境单独补充配置，不要把其他机器的固定路径、Node 版本命令或浏览器可执行文件路径直接写进本工程 README。

推荐保留以下原则：

- 只有在截图和代码阅读都不足以确认运行时事实时，才引入浏览器侧验证
- 浏览器侧验证优先用于核实 Console、Network 和 DOM 证据
- 浏览器侧验证结果应回写到任务简报或反馈单，而不是只停留在对话里
- 如果需要接入 Chrome DevTools MCP、Arc 或其他浏览器，应在本工程里按本机环境单独补充说明
