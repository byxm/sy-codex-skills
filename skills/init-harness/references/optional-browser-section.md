# 可选浏览器章节规则

默认初始化时，不写入浏览器调试工具、本机浏览器命令、MCP 配置或机器相关路径。

只有当用户明确要求“同时初始化浏览器验证规则”时，才在 README 中追加可选章节。

## 可写入内容

- 何时适合做浏览器侧验证
- 浏览器侧验证的目标是核实事实，不替代人工验收
- 用户应如何指定目标页面
- Agent 做完浏览器侧验证后，应该输出哪些证据和结论

## 不应写入内容

- 当前机器的 Node 版本命令
- 当前机器的 Arc / Edge 路径
- 当前机器的 MCP server 专属配置
- 与特定工程绑定的浏览器调试说明
